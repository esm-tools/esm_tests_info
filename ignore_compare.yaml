.run:
    - "echo $(date +%c) : compute"
    - 'echo $(date +"%a %b  %e %T %Y") :'
finished_config:
    - "    jobid: "
    - "    check: "
    - "    original_command: "
finished_config_dicts:
    - "files_missing_when_preparing_run"
