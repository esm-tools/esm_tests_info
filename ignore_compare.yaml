# Regular expresions for the ignore files
.run:
    - "echo $(date +%c) : compute"
    - 'echo \$\(date \+"%a %b  %e %T %Y"\) :'
finished_config:
    - "    jobid: "
    - "    check: "
    - "    original_command: "
    - "_glob_[1-9]*: "
# Literal key strings for the finished config
finished_config_dicts:
    - "files_missing_when_preparing_run"
